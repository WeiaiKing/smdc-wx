<view class="page">
    <view class="header flex">
        <image src="{{img}}" class="service_img" />
        <view class="flex flex-col">
            <text class="cell_text">呼叫服务</text>
            <text class="help_text">我们能帮您做什么</text>
        </view>
    </view>

    <view class="main">
        <service-item wx:for="{{ service }}" serviceItem="{{item}}" wx:key="id" class="service-item" />
    </view>
</view>